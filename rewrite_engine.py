from openai import OpenAI
import os
from dotenv import load_dotenv

load_dotenv()
client = OpenAI(api_key=os.getenv("OPENAI_API_KEY"))

def rewrite_news_humanized(text):
    prompt = """
    أعد صياغة الخبر التالي بأسلوب صحفي عربي بشري احترافي، قريب من كتابة وكالات الأنباء مثل رويترز و«أسوشيتد برس» و«سكاي نيوز عربية».

    القواعد الإلزامية:
    1. لغة بشرية طبيعية بدون نمط الذكاء الاصطناعي.
    2. استخدام مزيج من الجمل القصيرة والمتوسطة والطويلة.
    3. ترتيب الخبر بحيث يبدأ بالحدث الجديد الأهم ثم التفاصيل ثم الخلفية.
    4. تقديم المعلومات بموضوعية وهدوء مثل رويترز (لغة محايدة – دقيقة – بلا مبالغة).
    5. تجنب الجمل الإنشائية والنمطية (كرّس حياته، رمز للصمود، واصل المسيرة…).
    6. إضافة خلفية بسيطة فقط إن كان هناك سياق واضح.
    7. عدم اختراع معلومات جديدة إطلاقًا.
    8. منع التكرار والحشو.
    9. يمكن إضافة لمس
